{
  "name": "quotes-landing-v2",
  "version": "1.0.0",
  "description": "This project is a landing page, where is used such methodology as BEM(naming/components partially), LESS preprocessor, Gulp to convert and bundle all less files into one css and other features.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "gulp",
    "less",
    "BEM"
  ],
  "author": "bogdangill",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-less": "^4.0.1",
    "less": "^3.12.2",
    "normalize.less": "^1.0.0"
  }
}
